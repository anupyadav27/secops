{
  "rule_id": "repeated_patterns_in_regular_expressions_should_not_match_the_empty_string",
  "title": "Repeated patterns in regular expressions should not match the empty string",
  "type": "CODE_SMELL",
  "status": "ready",
  "defaultSeverity": "Minor",
  "description": "Avoid using the same pattern multiple times in a regular expression, especially when it matches the empty string. This can lead to unexpected behavior and performance issues.",
  "message": "Repeated pattern in regular expression matches the empty string.",
  "remediation": {
    "func": "Constant/Issue",
    "constantCost": "10min"
  },
  "tags": [
    "regex",
    "performance"
  ],
  "impact": "Performance and unexpected behavior",
  "recommendation": "Refactor the regular expression to avoid repeating the same pattern.",
  "references": [],
  "examples": {
    "noncompliant": [
      {
        "code": "import re\nregex = re.compile(r'(a|b)+')\nmatches = re.findall(regex, '')\n",
        "description": "Repeated pattern in regular expression matches the empty string."
      }
    ],
    "compliant": [
      {
        "code": "import re\nregex = re.compile(r'(a|b|c)+')\nmatches = re.findall(regex, '')\n",
        "description": "Use a different pattern to avoid repeating the same pattern."
      }
    ]
  },
  "devsecops": {
    "category": "",
    "security": "",
    "automation": "",
    "compliance": ""
  },
  "why_is_this_an_issue": "Repeated patterns can lead to performance issues and unexpected behavior when they match the empty string.",
  "how_to_fix": "Refactor the regular expression to avoid repeating the same pattern.",
  "security_mappings": {
    "CWE": "",
    "CERT": "",
    "OWASP": ""
  },
  "logic": {
    "check_type": "custom",
    "custom_function": "custom_check_repeated_empty_regex",
    "function_code": "def custom_check_repeated_empty_regex(node):\n    regex = ast.Regex(body=node.body)\n    if regex.pattern.count(r'') > 1:\n        return True\n    return False"
  },
  "metadata": {
    "filename": "repeated_patterns_in_regular_expressions_should_not_match_the_empty_string.json",
    "rule_id": "repeated_patterns_in_regular_expressions_should_not_match_the_empty_string",
    "title": "Repeated patterns in regular expressions should not match the empty string",
    "status": "ready",
    "defaultSeverity": "Minor"
  }
}